{
    "routes": [
      { "handle": "filesystem" },
      {
        "src": "/api/(.*)",
        "dest": "/api/:1"
      },
      {
        "src": "/socket/(.*)",
        "dest": "/socket/:1"
      },
      {
        "src": "/client/(.*)",
        "dest": "/client/:1"
      }
    ]
  }
  